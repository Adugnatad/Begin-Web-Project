import { createTheme } from "@mui/material";

export const Theme = createTheme({
  palette: {
    primary: {
      main: "#fff",
      light: "#242424",
    },
    secondary: {
      main: "#fff",
    },
    black: {
      main: "#000",
      light: "#242424",
    },
    theme: {
      main: "#1EE1B3",
    },
  },
});
